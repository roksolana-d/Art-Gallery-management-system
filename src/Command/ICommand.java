package Command;

/**
 * Interface for defining type of commands and main method to be executed
 * in each of them differently
 * @author user
 *
 */
public interface ICommand {

	public void execute();
}
